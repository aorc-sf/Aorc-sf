(()=>{
const SAVED_KEY='aorc_saved_posts_v1';let sortMode='recent',searchTerm='',saved=new Set(JSON.parse(localStorage.getItem(SAVED_KEY)||'[]'));
const feed=document.getElementById('feed'),boardBox=document.querySelector('#board>.box'),boardEl=document.getElementById('board'),composer=document.getElementById('composer'),tabs=document.querySelector('.tabs');if(!feed||!boardBox||!boardEl)return;
boardBox.id='boardFilters';if(tabs)tabs.classList.add('board-tabs-duplicate');
const labels={featured:'Featured',housing:'Housing',jobs:'Jobs',business:'Business',grants:'Grants & Funding',events:'Events',services:'Services & Resources',education:'Education',collaboration:'Collaboration',organizing:'Organizing',other:'Other'};
const order=['featured','housing','jobs','business','grants','events','services','education','collaboration','organizing','other'];
function postId(c){const b=[...c.querySelectorAll('button')].find(x=>/showComments\((\d+)\)/.test(x.getAttribute('onclick')||''));const m=b&&(b.getAttribute('onclick')||'').match(/showComments\((\d+)\)/);return m?Number(m[1]):0}
function postCategory(c){const bs=[...c.querySelectorAll('.badge')].map(x=>x.textContent.trim().toLowerCase());return Object.keys(labels).find(k=>k!=='featured'&&bs.includes(k))||'other'}
function isPinned(c){return [...c.querySelectorAll('.badge')].some(x=>x.textContent.trim().toLowerCase()==='pinned')}
function saveSet(){localStorage.setItem(SAVED_KEY,JSON.stringify([...saved]))}
function closePanels(except){[boardBox,composer].forEach(p=>{if(p&&p!==except)p.classList.remove('board-panel-open')})}
function injectQuickbar(){if(document.getElementById('boardQuickbar'))return;const q=document.createElement('div');q.id='boardQuickbar';q.className='board-quickbar';q.innerHTML=`<div class="board-title-chip">Community Board</div><button id="findPostsBtn" class="btn find-posts" type="button">⌕ Find posts</button><button id="newPostBtn" class="btn new-post" type="button">＋ New post</button><button id="messagesQuickBtn" class="btn muted" type="button">✉ Messages</button>`;boardEl.prepend(q);q.querySelector('#findPostsBtn').onclick=()=>{const open=!boardBox.classList.contains('board-panel-open');closePanels(boardBox);boardBox.classList.toggle('board-panel-open',open);if(open)setTimeout(()=>document.getElementById('boardSearch')?.focus(),50)};q.querySelector('#newPostBtn').onclick=()=>{if(typeof user==='undefined'||!user){status('Sign in to create a post.');document.querySelector('.safe+.box')?.scrollIntoView({behavior:'smooth',block:'start'});return}const open=!composer?.classList.contains('board-panel-open');closePanels(composer);composer?.classList.toggle('board-panel-open',open);if(open)setTimeout(()=>document.getElementById('title')?.focus(),50)};q.querySelector('#messagesQuickBtn').onclick=()=>typeof switchView==='function'&&switchView('messages')}
function injectControls(){if(document.getElementById('boardSearch'))return;const wrap=document.createElement('div');wrap.className='board-v2-tools';wrap.innerHTML=`<div class="board-search-row"><input id="boardSearch" type="search" placeholder="Search posts…"><select id="boardSort"><option value="recent">Most recent</option><option value="discussed">Most discussed</option><option value="saved">Saved first</option></select><button id="myCityBtn" class="btn muted small">My city</button><button id="myPostsBtn" class="btn muted small">My posts</button></div><div id="categoryChips" class="category-chips"></div>`;boardBox.appendChild(wrap);const cats=[['','All'],['housing','Housing'],['jobs','Jobs'],['business','Business'],['events','Events'],['grants','Grants'],['organizing','Organizing'],['services','Services'],['education','Education'],['collaboration','Collaboration']];const chips=wrap.querySelector('#categoryChips');cats.forEach(([v,l])=>{const b=document.createElement('button');b.className='category-chip'+(v===(document.getElementById('catfilter')?.value||'')?' active':'');b.textContent=l;b.onclick=()=>{document.getElementById('catfilter').value=v;[...chips.children].forEach(x=>x.classList.toggle('active',x===b));window.board()};chips.appendChild(b)});wrap.querySelector('#boardSearch').oninput=e=>{searchTerm=e.target.value.trim().toLowerCase();arrange()};wrap.querySelector('#boardSort').onchange=e=>{sortMode=e.target.value;arrange()};wrap.querySelector('#myCityBtn').onclick=()=>{const c=(typeof profile!=='undefined'&&profile?.city)||document.getElementById('pcity')?.value||'';if(c){document.getElementById('cityfilter').value=c;window.board()}};wrap.querySelector('#myPostsBtn').onclick=showMyPosts}
async function countComments(cards){const ids=cards.map(postId).filter(Boolean);if(!ids.length)return;try{const rows=await rest('community_comments',{q:'select=post_id&status=eq.active&post_id=in.('+ids.join(',')+')'}),counts={};(rows||[]).forEach(r=>counts[r.post_id]=(counts[r.post_id]||0)+1);cards.forEach(c=>{c.dataset.comments=counts[postId(c)]||0;const b=[...c.querySelectorAll('button')].find(x=>(x.getAttribute('onclick')||'').includes('showComments('));if(b&&!b.dataset.counted){b.textContent='Comments'+(+c.dataset.comments?' · '+c.dataset.comments:'');b.dataset.counted='1'}});if(sortMode==='discussed')arrange()}catch{}}
async function attachSourceLinks(cards){const ids=cards.map(postId).filter(Boolean);if(!ids.length)return;try{const rows=await rest('community_posts',{q:'select=id,business_url,is_official&id=in.('+ids.join(',')+')'}),byId={};(rows||[]).forEach(r=>byId[r.id]=r);cards.forEach(c=>{const id=+c.dataset.postId||postId(c),r=byId[id];if(!r?.business_url||c.querySelector('.official-source-link'))return;let u;try{u=new URL(r.business_url);if(!/^https?:$/.test(u.protocol))return}catch{return}const a=document.createElement('a');a.className='official-source-link';a.href=u.href;a.target='_blank';a.rel='noopener noreferrer';a.textContent=r.is_official?'↗ Official source / sign-up page':'↗ Website';const actions=c.querySelector('.actions');(actions||c).insertAdjacentElement(actions?'beforebegin':'beforeend',a)})}catch{}}
function decorate(){const cards=[...feed.querySelectorAll('.post')];let fresh=false;cards.forEach((c,i)=>{if(c.dataset.v2)return;fresh=true;c.dataset.v2='1';c.dataset.original=i;c.dataset.postId=postId(c);c.classList.remove('expanded');c.removeAttribute('tabindex');c.removeAttribute('role');c.removeAttribute('aria-expanded');const p=c.querySelector('p');if(p)p.classList.add('post-preview');c.querySelectorAll('.read-more').forEach(x=>x.remove());const a=c.querySelector('.actions'),id=+c.dataset.postId;if(a&&id){const s=document.createElement('button');s.className='btn muted small save-post';s.textContent=saved.has(id)?'★ Saved':'☆ Save';s.onclick=e=>{e.stopPropagation();saved.has(id)?saved.delete(id):saved.add(id);saveSet();s.textContent=saved.has(id)?'★ Saved':'☆ Save';arrange()};a.appendChild(s);const close=[...a.querySelectorAll('button')].find(b=>/closePost\(/.test(b.getAttribute('onclick')||''));if(close)close.textContent='✓ Mark filled'}});if(fresh){countComments(cards);attachSourceLinks(cards);arrange()}}
function sortCards(cards){return cards.sort((a,b)=>{if(sortMode==='discussed')return (+b.dataset.comments||0)-(+a.dataset.comments||0)||(+b.dataset.postId)-(+a.dataset.postId);if(sortMode==='saved')return (saved.has(+b.dataset.postId)?1:0)-(saved.has(+a.dataset.postId)?1:0)||(+b.dataset.postId)-(+a.dataset.postId);return (+a.dataset.original)-(+b.dataset.original)||(+b.dataset.postId)-(+a.dataset.postId)})}
function makeSection(key,cards){const sec=document.createElement('section');sec.className='board-section';sec.dataset.section=key;const h=document.createElement('div');h.className='board-section-head';h.innerHTML=`<h2 class="board-section-title">${labels[key]||key}</h2><span class="board-section-count">${cards.length}</span>`;const grid=document.createElement('div');grid.className='section-grid';cards.forEach(c=>grid.appendChild(c));sec.append(h,grid);if(cards.length>3){const t=document.createElement('button');t.className='section-toggle';t.textContent='View all '+cards.length;t.onclick=()=>{const open=sec.classList.toggle('expanded');t.textContent=open?'Show fewer':'View all '+cards.length};sec.appendChild(t)}return sec}
function arrange(){const cards=[...feed.querySelectorAll('.post')];if(!cards.length)return;cards.forEach(c=>c.hidden=false);const cat=document.getElementById('catfilter')?.value||'';let shown=cards.filter(c=>!searchTerm||c.innerText.toLowerCase().includes(searchTerm));shown=sortCards(shown);feed.innerHTML='';if(searchTerm||cat){const flat=document.createElement('div');flat.className='flat-feed';shown.forEach(c=>flat.appendChild(c));feed.appendChild(flat);return}const groups={};shown.forEach(c=>{const key=isPinned(c)?'featured':postCategory(c);(groups[key]||(groups[key]=[])).push(c)});order.forEach(k=>{if(groups[k]?.length)feed.appendChild(makeSection(k,groups[k]))})}
async function showMyPosts(){if(typeof user==='undefined'||!user){status('Sign in to manage your posts.');return}let panel=document.getElementById('myPostsPanel');if(!panel){panel=document.createElement('div');panel.id='myPostsPanel';panel.className='panel my-posts-panel';boardBox.after(panel)}panel.innerHTML='<strong>Loading your posts…</strong>';try{const rows=await rest('community_posts',{q:'select=id,title,category,city,status,created_at&author_id=eq.'+user.id+'&order=created_at.desc,id.desc'});panel.innerHTML='<div class="my-posts-head"><h3>My posts</h3><button class="btn muted small" id="closeMyPosts">Close</button></div>'+((rows||[]).map(p=>`<div class="my-post-row"><div><strong>${esc(p.title)}</strong><div class="meta">${esc(p.category)} · ${esc(p.city)} · ${p.status==='active'?'Active':'Filled'}</div></div><div class="row">${p.status==='active'?`<button class="btn muted small" onclick="markFilled(${p.id})">Mark filled</button>`:`<button class="btn secondary small" onclick="reactivatePost(${p.id})">Reactivate</button><button class="btn muted small" onclick="repostPost(${p.id})">Repost</button>`}</div></div>`).join('')||'<div class="help">No posts yet.</div>');document.getElementById('closeMyPosts').onclick=()=>panel.remove()}catch(e){panel.innerHTML='<div class="notice">'+esc(e.message)+'</div>'}}
window.markFilled=async id=>{await rest('community_posts',{method:'PATCH',q:'id=eq.'+id,body:{status:'filled',updated_at:new Date().toISOString()}});await window.board();document.getElementById('myPostsPanel')&&showMyPosts()};window.closePost=window.markFilled;window.reactivatePost=async id=>{await rest('community_posts',{method:'PATCH',q:'id=eq.'+id,body:{status:'active',updated_at:new Date().toISOString()}});await window.board();showMyPosts()};window.repostPost=async id=>{const rows=await rest('community_posts',{q:'select=*&id=eq.'+id}),p=rows?.[0];if(!p)return;await rest('community_posts',{method:'POST',body:{author_id:user.id,city:p.city,category:p.category,title:p.title,body:p.body,business_name:p.business_name||null,business_url:p.business_url||null,status:'active'}});await window.board();showMyPosts()};
injectQuickbar();injectControls();boardBox.classList.remove('board-panel-open');composer?.classList.remove('board-panel-open');const mo=new MutationObserver(()=>{clearTimeout(window.__aorcDecorateTimer);window.__aorcDecorateTimer=setTimeout(decorate,30)});mo.observe(feed,{childList:true});decorate();
})();