document.addEventListener('click',function(e){const post=e.target.closest('.post');if(!post)return;if(e.target.closest('button,a,input,textarea,select,label'))return;post.classList.toggle('expanded');post.setAttribute('aria-expanded',post.classList.contains('expanded')?'true':'false');});
document.addEventListener('keydown',function(e){if(e.key!=='Enter'&&e.key!==' ')return;const post=e.target.closest('.post');if(!post||e.target!==post)return;e.preventDefault();post.classList.toggle('expanded');post.setAttribute('aria-expanded',post.classList.contains('expanded')?'true':'false');});
document.addEventListener('DOMContentLoaded',function(){document.querySelectorAll('.post').forEach(p=>{p.setAttribute('tabindex','0');p.setAttribute('role','button');p.setAttribute('aria-expanded','false');});const feed=document.getElementById('feed');if(feed){new MutationObserver(()=>document.querySelectorAll('.post').forEach(p=>{if(!p.hasAttribute('tabindex')){p.setAttribute('tabindex','0');p.setAttribute('role','button');p.setAttribute('aria-expanded','false');}})).observe(feed,{childList:true,subtree:true});}});