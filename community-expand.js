document.addEventListener('DOMContentLoaded',function(){const prep=()=>document.querySelectorAll('.post').forEach(p=>{p.removeAttribute('role');p.removeAttribute('aria-expanded');p.removeAttribute('tabindex');p.classList.remove('expanded');});prep();const feed=document.getElementById('feed');if(feed)new MutationObserver(prep).observe(feed,{childList:true,subtree:true});});